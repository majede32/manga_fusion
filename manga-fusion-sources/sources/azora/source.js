function createSource() { return { async getLatestManga(page) { const res = await fetch('https://api.azorafly.com/api/v1/manga/latest?page=' + page); const json = await res.json(); return { success: true, data: json.mangas.map(i => ({id: i.slug, title: i.title, cover: i.coverUrl})) }; }, async getMangaDetails(mangaId) { const res = await fetch('https://api.azorafly.com/api/v1/manga/' + mangaId); const json = await res.json(); return { success: true, data: { title: json.title, chapters: json.chapters.map(c => ({id: c.id, number: c.number})) } }; }, async getChapterImages(chapterId) { const res = await fetch('https://api.azorafly.com/api/v1/chapter/' + chapterId); const json = await res.json(); return { success: true, data: json.images }; } }; }
