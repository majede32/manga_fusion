function createSource() { const headers = { 'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124.0.0.0 Safari/537.36', 'Referer': 'https://lek-manga.net/' }; return { async getLatestManga(page) { const res = await fetch('https://lek-manga.net/manga?page=' + page, { headers }); const html = await res.text(); return { success: true, data: [] }; }, async getChapterImages(url) { const res = await fetch(url, { headers }); const html = await res.text(); const regex = /<img[^>]+src="([^"]+)"/g; let match, images = []; while (match = regex.exec(html)) { images.push(match[1]); } return { success: true, data: images }; } }; }
